// Task 1: Create an Inventory Array of Product Objects

let inventory = [
    { name: 'Refridgerator', price: 1800, quantity: 8, lowStockLevel 2 },
    { name: 'Microwave', price: 600, quantity: 7, lowStockLevel: 4},
    { name: 'Washing Machine', price: 2000, quantity: 1, lowStockLevel: 8},
    { name: 'Televison', price: 400, quantity: 5, lowStockLevel: 3},
    { name: 'Drying Machine', price: 700, quantity: 6, lowStockLevel: 1},


];

// Task 2: Create a Function to Display Product Details

{ function displayProductDetails(inventory) = {
    const [name, price, quantity, lowStockLevel] = Refrigerator;
    console.log(inventory.name: ${refrigerator})};
    console.log(inventory.price: ${1800tofixed})};
    console.log(inventory.quantity: ${(stockstatus2)};

};


// Task 3: Create a Function to Update Product Stock After Sales:

function updateStock(inventory) = {
    const [20(unitsSold)]
   console.log(quantity(1800) - unitsSold(20)) = 1780
(out of stock)

};

// Task 4: Create a Function to Check Low Stock Products:

function checkLowStock (inventory) {
    var (totalvalu calculateInventoryValue(price: 1800 * quantity: 8) = 14,400) = Refrigerator
    var( totalvalue calcualteInventoryValue(price: 600 * quantity: 7) = 4,200) = Microwave

};

// Task 6: Create a Function to Process a Sale:

function processSale (productName) = {
    var productName = inventory.find((product=))
}



    
